{
  "build": {
    "dockerfile": "../ubuntu/Dockerfile",
    "context": "../ubuntu"
  },
  "runArgs": [
    "--name",
    "tgotwig-dev-images",
    "--network=host"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.minimumContrastRatio": 1
      }
    }
  }
}

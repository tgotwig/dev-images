{
  "build": {
    "dockerfile": "../ubuntu/Dockerfile",
    "context": "../ubuntu"
  },
  "runArgs": [
    "--name=tgotwig-dev-images",
    "--network=host",
    "-v",
    "/var/run/docker.sock:/var/run/docker.sock"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.minimumContrastRatio": 1
      }
    }
  }
}
